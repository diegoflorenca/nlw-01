import React from 'react';

// // Essa interface descreve quais 'atributos' (e seus tipos) que esse componente pode receber
// interface HeaderProps {
//   title: string; // Para tornar um 'atributo' opcional basta inserir um ? antes do : assim -> title?: string;
// }

// // Com o React.FC<> indicamos qual interface pertence a esse componente <HeaderProps> e dentro dos parâmentros da função (props) podemos ter acesso a todos os valores da interface
// const Header: React.FC<HeaderProps> = (props) => {
//   return (
//     <header>
//       <h1>{props.title}</h1>
//     </header>
//   );
// };

// export default Header;
